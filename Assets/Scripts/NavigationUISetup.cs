
using UnityEngine;

public class NavigationUISetup : MonoBehaviour
{
    // ✅ For now, this script is just a placeholder.
    // All service popup logic has been removed.
}

// Keep the WaypointType enum here (or move it to its own file if you prefer)
public enum WaypointType
{
    Office,
    Corridor,
    Junction,
    Stairs,
    Entrance,
    Exit
}
